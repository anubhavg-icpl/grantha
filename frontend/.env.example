# Grantha Frontend Environment Variables
# NOTE: This frontend now uses the unified .env file from the project root
# Copy the root .env.example to .env in the project root and configure there

# These are legacy variables for reference - use root .env instead:
# PUBLIC_API_BASE_URL=http://localhost:8000/api
# PUBLIC_WS_URL=ws://localhost:8000/ws
# PUBLIC_APP_NAME=Grantha
# PUBLIC_APP_VERSION=1.0.0

# The frontend Vite configuration automatically loads from ../env
# Environment variables are accessed through the unified configuration

# Development override (optional - creates local .env.local if needed)
# NODE_ENV=development
# VITE_LOG_LEVEL=info